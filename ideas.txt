-Compute covariance matrix and correlation between assets
-PNL distribution function using histogram of returns

BOOTSTRAPPING
60% training 20% validation, 20% testing set
use rolling/expanding window to redetermine weights
metrics: out of sample sharpe/sortino, max DD, turnover

Block bootstrapping: Resample historical periods in blocks (e.g. 1–3 months) to preserve correlations, run 100–500 simulations. Check DD/cap metrics distribution.
Stress tests: Force‑feed known crises (2008 GFC, 2020 COVID, 2022 inflation/rates) + synthetic (e.g. +3std vol spikes).
Compare multiple objectives: Test 3–5 variants (Sharpe, Sortino, min DD, CVaR) side‑by‑side on the same OOS data; pick the most stable.

OPTIMALIZATION:
-Cvar_alpha
-max_w{w^T*mu} subject to Cvar_alpha(w) <= c and volatility <= sigma^2
-max_w{w^T*mu - lambda*Cvar_alpha(w)}
-max_w{w^T*mu - lambda*Cvar_alpha(w) - gamma*w^T*Sigma*w} (added volatility)